# clouderby Server App Configuration for CloudHub

http:
  port: "8081"

# Derby embedded connection (directory-based)
derby:
  path: "/tmp/data/appdb"

# Authentication
clouderby:
  user: "mule"
  password: "mule123"

# Database initialization (CNET schema + sample data)
db:
  init:
    enabled: "true"
